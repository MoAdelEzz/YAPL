float test_function(float a, float b, float n, float m) {
    float result = a;

    if (result >= 4.0) {
        return result;
    } else if (result >= 2.0) {
        return result;
    } else {
        return -3.0;
    }

    return -4.0;
}

float a = test_function(4.0, 1.0, 1.0, 1.0);
print("a = " + a);

float b = test_function(3.0, 1.0, -1.0, -4.0);
print("b = " + b);


float c = test_function(2.0, 1.0, -1.0, -4.0);
print("c = " + c); 